# Plan

1. Inspect the dependency diff, release scope, and current PR state.
2. Update the dependency ledger and this feature-memory task list.
3. Run the full local CI command and push only scoped corrections.
4. Request a current-head Codex review and resolve blocking findings.
5. Merge only after every required gate is green and the safety delay passes.
